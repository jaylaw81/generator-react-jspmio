import React from 'react';

class <%= componentName %> extends React.Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <<%= componentName %> />
    );
  }
}

export default <%= componentName %>;
